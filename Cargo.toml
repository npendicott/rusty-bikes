[package]
name = "rusty-bikes"
version = "0.1.0"
edition = "2024"

[dependencies]
reqwest = { version = "0.13", features = ["blocking", "json"] }
csv = "*"
serde = { version = "1.0", features = ["derive"] }
serde-xml-rs = "0.8"
bytes = "*"
